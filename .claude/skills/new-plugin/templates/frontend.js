export const translations = {
  en: {
    {{PLUGIN_ID}}: "{{PLUGIN_NAME}}",
  },
  ja: {
    {{PLUGIN_ID}}: "{{PLUGIN_NAME}}",
  },
};

export function register(host) {
  // TODO: register view/edit panels via the panel host.
  // host.registerViewPanel("{{PLUGIN_ID}}_summary", renderSummary);
  // host.registerEditPanel("{{PLUGIN_ID}}_metadata", renderEditor);
}
